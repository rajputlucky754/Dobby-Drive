PORT=4000
MONGODB_URI=mongodb://localhost:27017/dobby_drive
JWT_SECRET=super_secret_change_me
CLIENT_ORIGIN=http://localhost:5173

# Storage: set to true to use Cloudflare R2 (S3-compatible)
USE_S3=true

# Cloudflare R2 (S3-compatible) credentials
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=auto
S3_BUCKET=
# Endpoint should be like: https://<account-id>.r2.cloudflarestorage.com
S3_ENDPOINT=
# Public base URL to serve files (optional). If omitted, the code will default to `${S3_ENDPOINT}/${S3_BUCKET}`
S3_PUBLIC_URL=
